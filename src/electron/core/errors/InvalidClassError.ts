import BaseError from "./BaseError.js";

/**
 * A specific name that clearly indicates the issue is with the provided class implementation.
 */
export default class InvalidClassError extends BaseError {}
